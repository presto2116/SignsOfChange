


<%= @question.question %>

<%= form_for [@student, @question, @question_response] do |f| %>

	<%= f.label :response, "Yes", :value => "true" %>
	<%= f.radio_button :response, true, :checked => true %>
	<%= f.label :response, "No", :value => "false" %>
	<%= f.radio_button :response, false %>
	<%= f.submit "submit"%>


<% end %>

