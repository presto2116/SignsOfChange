<% @signs.each do |sign| %>
  <img src="<%= sign.sign_text %>" height="42" width="42"/>
  <!-- <%= form_tag sign_path(1), method: "delete" do %>
  <%= submit_tag("delete this sign")%> -->
  <%end%>

  <%end%>

<br>
<%= link_to "Add your own selfie sign", new_sign_path %>
<%= link_to "Back to resources", students_path %>
